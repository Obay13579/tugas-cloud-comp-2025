server {
    listen 80;
    server_name _;

    root /app/site;
    index index.html;

    location / {
        try_files $uri $uri/ =404;
    }

    location /api/feedback {
        proxy_pass http://127.0.0.1:5000;
    }
}
